# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Python
fastapi/.venv/
__pycache__/
*.pyc

# Node leftovers (in case anyone npm-installs by mistake)
/node_modules

# OS / editor
.DS_Store
*.pem

# Env (opt-in: commit only the example)
.env*
!.env.example

# Service account key (never commit)
key.json
